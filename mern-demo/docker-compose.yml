version: "3.8"

services:
  backend:
    build: ./server
    container_name: mern-backend
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - MONGO_URI=mongodb+srv://s236945_db_user:chinguyen126@cluster0.crgsvyq.mongodb.net/?appName=Cluster0

  frontend:
    build: ./client
    container_name: mern-frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend
